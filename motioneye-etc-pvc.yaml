kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: motioneye-etc-pvc
  namespace: motioneye
  labels:
    app: motioneye
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi